Tengo que cambiar de SQLite a PostgrSQL para contenerla en un docker y creo que con postgres es mucho mas estable, ayudame para poder cambiar las bases de datos y poder dockerizarlo

estoy preparando el Dockerfile para empaquetar una app en .NET y quiero usar una construcción multietapa para que la imagen final sea mas liviana. me puedes explicar que imagenes base deberia usar de .NET?

tengo que agregar la instrucción HEALTHCHECK dentro de mi Dockerfile para comprobar que la app responde bien en el endpoint /health, como puedo hacerlo?

Como se debe hacer un .dockerignore para este caso en el que estoy trabajando, lo que debereia contener si o si